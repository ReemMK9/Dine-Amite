import React from 'react'
import styles from './PrimaryButton.module.css'

const PrimaryButton = () => {
  return (
    <button className={styles.primaryButton}>PrimaryButton</button>
  )
}

export default PrimaryButton